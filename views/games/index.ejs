<%- include('../partials/header') %>
<table>
  <h1>Games List</h1>
  <tr>
    <th>Remove:</th>
    <th>Version Title:</th>
    <th>Release year</th>
    <th>Console:</th>
    <th>Trainer Name</th>
  </tr>
</thead>

  <% games.forEach (function(g) { %>

<tbody>
    <tr>
    <td><form action="/games/<%= g.id %>?_method=DELETE"  class="delete-form"   method="POST">
     <button type="submit">X</button>
    </form></td>
  
    <td><%= g.versionTitle %></td>
    <td><%= g.releaseYear %></td>
    <td><%= g.console %></td>
    <td><%= g.trainerName %></td>
  </tr>
  <% }); %>

</tbody>

</table>
<%- include('../partials/footer') %>
